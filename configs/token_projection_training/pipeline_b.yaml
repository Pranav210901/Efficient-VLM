extends: pipeline_a.yaml
version: token-projection-training-b-1.0
output_root: results/token_projection_training/B_learned_query_attention
checkpoint_root: checkpoints/token_projection_training/B_learned_query_attention
base_config: configs/token_projection_training/base_b.yaml
wave1:
  enabled: true
  design: token_projection_training
  question: Does learned-query cross-attention pooling improve transfer without measurable latency cost?
  locked_recipe:
    {loss_type: infonce_no_queue, captions_per_image: null, memory_queue_size: 0, batch_size: 1024, sweep_multiplier: 3.0, image_size: 224}
teacher_extension:
  arm_id: B_learned_query_attention
  label: "224px MobileCLIP2-distilled learned-query attention pooling"
  teacher_ids: [mobileclip2_s0_dfndr2b]
token_projection:
  candidate: B
  image_token_aggregation: learned_query_attention
  pool_dim: 128
  heads: 4
  register_tokens_included: false
  expected_inference_trainable_parameters: 1644164
  lr_tuned_for_aggregator: false
